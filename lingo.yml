input:
  path: /path/to/csv

platforms:
  - platform: iOS
    module: plugins.ios.lingo_ios
    plugin: LingoIOS
    # the directory where the .xcodeproj file is located.
    proj_root_path: /path/to/ios/project/path
    # 'append' or 'cover', default is 'append'
    mode: append # cover

  - platform: Flutter
    module: plugins.flutter.lingo_flutter
    plugin: LingoFlutter
    # the directory where the flutter project is located.
    proj_root_path: /path/to/flutter/project/path
